# GitPull 
#!/bin/bash
# Pull and update from GitHub

# Fail immediately if a command errors
set -e

echo "Fetching latest changes..."
git fetch origin

echo "Pulling into current branch..."
git pull origin "$(git rev-parse --abbrev-ref HEAD)"

echo "Repository updated successfully."
